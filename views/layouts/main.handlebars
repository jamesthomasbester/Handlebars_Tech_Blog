<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/dashboard.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
  
</head>

<body>
    <header class="p-3 bg-dark text-white">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <h2 class="px-4">Tech Blog</h2>
                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li>
                        <a href="/" class="nav-link px-2 text-white">Home</a>
                    </li>
                    <li>
                        <a href="/dashboard" class="nav-link px-2 text-white">Dashboard</a>
                    </li>
                </ul>
                <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" role="search">
                    <input type="search" class="form-control form-control-dark text-white bg-dark" placeholder="Search..." aria-label="Search">
                </form>
                <div class="text-end d-flex">
                    {{#if logged_In}}
                    <form action="/api/users/logout"  method="post" class="me-2">
                        <button type="submit" class="btn btn-outline-light " href="/logout">Logout</button>
                    </form>
                    {{else}}
                        <form action="/login" class="me-2">
                            <button type="submit" class="btn btn-outline-light " href="/login">Login</button>
                        </form>
                        <form action="/signup" class="">
                            <button class="btn btn-warning" href="/signup">Sign-up</button>
                        </form>
                    {{/if}}
                </div>
            </div>
        </div>
        <div class="b-example-divider">

        </div>
    </header>
  <main class="">
      {{{ body }}}
  </main>
  <footer>
      
  </footer>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script src="js/comment.js"></script>
</body>

</html>
